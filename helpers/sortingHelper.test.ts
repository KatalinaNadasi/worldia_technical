import { sortByAttributes, sortingOrder } from "./sortingHelper";

describe("should sort the list of data by attributes in given order", () => {
  it("should sort the data by ascending & descending order of airline names", () => {});

  it("should sort the data by ascending & descending order of price", () => {});

  it("should sort the data by ascending & descending order of duration", () => {});
});
